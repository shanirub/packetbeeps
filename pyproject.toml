[tool.poetry]
name = "packetbeeps"
version = "0.1.0"
description = "sniffer that also beeps"
authors = ["Shani Rubovitch"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.9"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
